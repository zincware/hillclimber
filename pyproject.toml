[project]
name = "plumed-nodes"
version = "0.1.0a6"
description = "Python interfaces for the plumed library."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ase>=3.25.0",
    "plumed>=2.9.0",
    "rdkit2ase>=0.1.13",
    "zntrack>=0.8.9",
]


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "ipykernel>=6.30.1",
    "pytest>=8.4.1",
]

[project.entry-points.'zntrack.nodes']
plumed-nodes = 'plumed_nodes.nodes:nodes'

[project.entry-points.'zntrack.interfaces']
plumed-nodes = 'plumed_nodes.interfaces:interfaces'
